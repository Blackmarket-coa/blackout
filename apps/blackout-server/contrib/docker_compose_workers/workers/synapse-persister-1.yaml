worker_app: synapse.app.generic_worker
worker_name: synapse-persister-1

worker_replication_host: synapse
worker_replication_http_port: 9093

worker_listeners:
  - type: http
    port: 8084
    x_forwarded: true
    resources:
      - names: [health]

worker_log_config: /data/persister_worker.log.config

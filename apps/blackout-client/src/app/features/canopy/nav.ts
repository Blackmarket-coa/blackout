import type { FeatureNavItem } from '../../core/features/types';
import { CANOPIES_PATH } from '../../pages/paths';

export const canopyNavItems: FeatureNavItem[] = [{ label: 'Canopies', to: CANOPIES_PATH }];

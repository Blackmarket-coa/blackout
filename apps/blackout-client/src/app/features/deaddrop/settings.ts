import type { FeatureSettingsItem } from '../../core/features/types';
import { DeadDropRoutePage } from './routes';

export const deaddropSettings: FeatureSettingsItem[] = [
    { section: 'Dead Drop', component: DeadDropRoutePage },
];

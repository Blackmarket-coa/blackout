import type { FeatureSettingsItem } from '../../core/features/types';
import { DeadmanRoutePage } from './routes';

export const deadmanSettings: FeatureSettingsItem[] = [
    { section: 'Deadman Switch', component: DeadmanRoutePage },
];

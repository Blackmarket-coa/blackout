import PlaceholderSection from './PlaceholderSection';

const AccessibilitySettings = () => (
    <PlaceholderSection
        title="Accessibility"
        description="Improve readability and reduced sensory load."
        items={['Reduced motion', 'Screen reader', 'High contrast']}
    />
);

export default AccessibilitySettings;

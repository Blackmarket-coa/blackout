Environment contract:

- `VITE_MATRIX_HOMESERVER_URL`: full homeserver URL (preferred).
- `BLACKOUT_SERVER_URL`: fallback homeserver URL.
- Railway shorthand is supported as `railway:<service-name>` and resolves to `https://<service-name>.up.railway.app`.

import { describe, it } from 'vitest';

describe('feature-epic-delivery-blueprint-unavailable', () => {
  it('documents unavailable UI automation coverage for this feature id', () => {
    // Placeholder coverage marker for guard:feature-budget.
  });
});

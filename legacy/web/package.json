{
  "name": "@blackout/web",
  "version": "0.0.1",
  "private": true,
  "description": "Legacy web shell (non-deploy); canonical frontend is @blackout/client",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "dev": "echo 'no dev server yet'",
    "lint": "tsc --noEmit",
    "test": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@blackout/core": "workspace:*",
    "@blackout/ui": "workspace:*",
    "@blackout/design": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}

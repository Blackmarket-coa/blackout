# Legacy blackout/ snapshot (non-workspace)

This directory is retained as historical migration context only.

## Important

- The canonical monorepo root is **repository root (`/workspace/blackout`)**.
- Do **not** run `pnpm` or `turbo` from this folder.
- Do **not** add `package.json`, `pnpm-workspace.yaml`, or `turbo.json` here.

## Where to work

Use these canonical paths from repo root:

- `apps/blackout-client`
- `apps/blackout-server`
- `packages/*`
- `infra/*`
- `docs/*`

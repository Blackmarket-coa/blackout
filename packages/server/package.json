{
  "name": "@blackout/server-legacy-alias",
  "version": "0.0.1",
  "private": true,
  "description": "Legacy alias package (non-deploy); use apps/blackout-server package @blackout/server",
  "scripts": {
    "dev": "pnpm --filter @blackout/api run dev",
    "build": "pnpm --filter @blackout/api run build",
    "lint": "pnpm --filter @blackout/api run lint",
    "test": "pnpm --filter @blackout/api run test"
  }
}
